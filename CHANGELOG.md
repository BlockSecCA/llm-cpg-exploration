# Changelog

## 2026-04-18

- Added scroll-site companion animation: `animation.html` (single-file viewer) and `animation/` source scenes.
- Slide 3 Venn redrawn: labels in each non-overlap lobe, "BUG FINDING (solved)" in the LLM∩SAST lens, "?" in the 3-way intersection, side callout with arrow for "DESIGN ANALYSIS LIVES HERE". Ported from the animation's scene 3.
- Added v2 animation alongside v1: `animation-v2.html` (single-file viewer) and `animation-v2/` source. Adds a Web Audio engine (`audio.jsx`) with ambient pad + synthesized cues, optional narration auto-load (`narration.mp3`), `NARRATION_SCRIPT.md`, and ElevenLabs paste-ready script (`NARRATION_ELEVENLABS.md`).
